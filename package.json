{
  "name": "erp",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm --parallel build",
    "dev": "pnpm --parallel -r dev",
    "docker:build": "docker build --platform linux/amd64 -t 192.168.0.2:5000/erp:1.0.0 . --push"
  },
  "packageManager": "pnpm@9.1.1",
  "devDependencies": {
  }
}
